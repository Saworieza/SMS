<h1>Users</h1>
<% @users.each do |user| %>
	    <%= image_tag %>
	    <%= user.email %>
	    <%= link_to 'Add friend' %>
        <!--<td><%#=truncate(strip_tags(user.bio), length: 140) %></td>-->
<% end %>
 